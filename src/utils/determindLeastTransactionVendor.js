const determineLeastTransactionVendor = (products, orderHistories) => {
  let leastTransactionVendor = null;
  let minTransactions = Infinity;
};

module.exports = { determineLeastTransactionVendor };
